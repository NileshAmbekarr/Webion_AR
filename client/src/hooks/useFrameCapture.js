// Placeholder — Track A (Agent 1) implements this hook
// See docs/TRACK_A_C_GUIDE.md for full specification
export function useFrameCapture(sellerVideoRef, isCapturing) {
  return {
    captureStatus: 'idle',
    countdown: 0,
    qualityWarnings: [],
  };
}
